services:
  web:
    container_name: reservation-system
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./app:/app
      - app_data:/app/data
    command: gunicorn config.wsgi:application --bind 0.0.0.0:3001 --workers 3 --reload

volumes:
  app_data:
